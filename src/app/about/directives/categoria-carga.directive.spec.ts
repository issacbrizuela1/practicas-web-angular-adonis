import { CategoriaCargaDirective } from './categoria-carga.directive';

describe('CategoriaCargaDirective', () => {
  it('should create an instance', () => {
    const directive = new CategoriaCargaDirective();
    expect(directive).toBeTruthy();
  });
});
